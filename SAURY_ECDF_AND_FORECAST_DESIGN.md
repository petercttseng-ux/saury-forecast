# 西北太平洋秋刀魚 ECDF 分析與漁海況速報系統設計

更新日期：2026-09-22

## 1. 分析目的與資料範圍

`Saury-csv.txt` 包含 2006–2010 年、6–11 月共 2,879 筆臺灣秋刀魚漁業正 CPUE 作業紀錄，欄位為年月、經緯度、CPUE、SST、Chl-a、SSHA、EKE、NPP、MLD 與 100 m 水溫。所有欄位均無缺值，亦無完全重複列；有 57 筆重複的「年、月、緯度、經度」鍵，但其環境值或 CPUE 不完全相同，推測是同一月／位置的重複作業觀測，因此保留並列為資料限制。

這是一份只有正 CPUE 與實際作業位置的資料，缺少未作業海域與零漁獲樣本。因此可建立「相對棲地適合度」，不能把 0–1 分數解讀為秋刀魚出現率或捕獲率。

## 2. CPUE 加權 ECDF 方法

對每一環境參數 `x` 建立一般 ECDF `F(x)`，並以 CPUE 為權重建立 `Gcpue(x)`。`|F(x)-Gcpue(x)|` 最大的位置代表環境分布與高 CPUE 分布差異最明顯的指標點。此作法與 Tseng et al. (2013) 所述的 CPUE 加權 ECDF 邏輯一致；該研究以 SST、Chl-a 與 NPP 得到 14–16 °C、0.4–0.6 mg m⁻³、600–800 mg C m⁻² d⁻¹ 的高 CPUE 範圍。

作業圖的相對 HSI 以 0.1 為分類間距（0.0–0.1 至 0.9–1.0）；只有 HSI 嚴格大於 0.5 且連通面積至少 1,500 km² 的海域列為推薦漁場。

本系統另定義可重現的環境窗：

- 核心環境窗：CPUE 加權 ECDF 的 P25–P75。
- 可能環境窗：CPUE 加權 ECDF 的 P10–P90。
- 單變數適合度：核心窗內為 1；在 P10–P25 與 P75–P90 線性遞減；窗外為 0。
- 多變數 HSI：可用且時效合格的單變數適合度取幾何平均。

## 3. 由 `Saury-csv.txt` 得到的環境參數範圍

| 參數 | CPUE 加權核心 P25–P75 | 可能 P10–P90 | 最大 ECDF 差異位置 | 單位 |
| --- | ---: | ---: | ---: | --- |
| 海面水溫 SST | 13.5–16.6 | 12.2–18.0 | 15.2 | °C |
| 葉綠素 a | 0.41–0.77 | 0.30–1.10 | 0.54 | mg m⁻³ |
| 海面高度異常 SSHA | 3.77–11.87 | -0.19–15.83 | 1.40 | cm |
| 渦動動能 EKE | 10.9–111.1 | 4.0–262.0 | 23.2 | cm² s⁻² |
| 淨初級生產力 NPP | 527.8–895.0 | 417.9–1214.3 | 1178.9 | mg C m⁻² d⁻¹ |
| 混合層深度 MLD | 14.3–26.6 | 11.1–37.9 | 20.4 | m |
| 100 m 水溫 | 3.0–6.3 | 2.2–8.8 | 3.2 | °C |

注意：最大 ECDF 差異位置是一個診斷點，不等於完整適合範圍。歷史研究的 SST、Chl-a、NPP 範圍可作外部合理性比較，但不能直接取代本資料的加權分位數，也不能在未驗證前混合成同一套門檻。

## 4. 海洋遙測與作業海況資料源

### 4.1 目前正式使用的 JMA NEAR-GOOS 產品

| 產品 | 用途 | 網格與涵蓋範圍 | 更新 |
| --- | --- | --- | --- |
| [HIMSST `him_sst_pac_D`](https://www.data.jma.go.jp/goos/data/pub/JMA-product/him_sst_pac_D/) | 每日 SST、溫度鋒面、HSI 主因子 | 0.1°，**0–60°N, 100–180°E** | 每日 |
| [NPRSUBT `npr_subt_jpn_D`](https://www.data.jma.go.jp/goos/data/pub/JMA-product/npr_subt_jpn_D/) | 50/100/200/400 m 水溫；100 m 為 HSI 第二因子 | 0.1°×1/11°，16.8–56.2°N, **113.5–163.45°E** | 每日 |
| [NPRSUBC `npr_subc_jpn_D`](https://www.data.jma.go.jp/goos/data/pub/JMA-product/npr_subc_jpn_D/) | 表面海流向量、鋒面與漂移判讀 | 0.1°×1/11°，16.75–56.25°N, **113.5–163.5°E** | 每日 |
| [MGDSST `mgd_sst_glb_D`](https://www.data.jma.go.jp/goos/data/pub/JMA-product/mgd_sst_glb_D/) | HIMSST 遲到或雲隙破洞時的備援與補值 | 0.25°，全球 | 每日 |

### 4.2 涵蓋範圍的硬限制（系統設計的關鍵約束）

系統顯示與分析範圍為 **17–56°N, 114–180°E**。這個範圍內有兩種資料狀態：

- **114–163.45°E**：SST 與 100 m 水溫皆有 → HSI 為 **SST × 100 m 水溫**雙因子。
- **163.45–180°E**：只有 SST。JMA NEAR-GOOS 產品目錄中 **沒有任何涵蓋此範圍的次表層水溫或表面海流產品**（`npr_*_jpn_D` 的 `jpn` 即「日本近海」域）→ HSI **自動逐格降階為 SST 單因子**。

系統的處理原則：

1. 逐格判定模式組成，不是整張圖一體適用；介面與報告顯示雙因子／單因子的網格占比。
2. **不以邊界值向東外推填滿**次表層或海流場。沒有資料就是沒有資料。
3. 地圖上以虛線標出 163.45°E 界線並附說明。

秋刀魚主漁場在 9–11 月常延伸至 160–175°E，因此 163.45°E 以東的單因子區並非邊陲，而是漁季後段的核心作業海域；使用者必須知道該區的 HSI 少了一個因子。

### 4.3 已完成環境窗分析但尚未納入每日 HSI 的參數

Chl-a、NPP、SSHA、EKE 與 MLD 的 CPUE 加權環境窗已完成（見 §3），但要納入每日 HSI 需先建立同日期／同網格的資料管線並完成獨立回溯驗證。可用來源為 Copernicus Marine（Ocean Colour GLO L4 NRT 009_102、Sea Level L4 NRT 008_046、Global Physics Forecast 001_024）與 NASA Ocean Color；這些需要帳號憑證，憑證不得寫入靜態網站或版本庫，靜態頁面只接收經伺服器裁切、量化後的網格 JSON。

## 5. 速報與三日預報系統架構

### 5.1 資料流

```text
JMA NEAR-GOOS（HIMSST / NPRSUBT / NPRSUBC / MGDSST）
          │  data_downloader.py（重試 + 主站/備援站切換）
          ▼
向量化解析 grid_io.py / data_parser.py（固定欄寬，右靠含前置空白）
          │
          ▼
品質閘門：範圍、缺值、覆蓋率、時效、單位、網格方向
          │
          ├─ HIMSST 缺值 → MGDSST 補值（記錄補值比例）
          ▼
統一裁切至 17–56°N, 114–180°E（保留各來源原始日期）
          │
          ├── 當日速報 D0：直接使用分析場
          └── 預報 D+1~D+3：forecast.py 外推
                    │
                    ▼
        SST 梯度／等溫線／海流／HSI（瀏覽器端即時計算）
                    │
                    ▼
        熱區連通元件、面積與航線優先序 → Leaflet 互動地圖 + HTML 報告
```

### 5.2 三日預報：方法與選定過程

**JMA NEAR-GOOS 沒有公開的海況預報產品。** 產品目錄只有分析場（MGDSST、HIMSST、COBE-SST2、NPR-4DVAR、MOVE/MRI.COM）與海冰圖。因此 D+1~D+3 必須由分析場外推。

採用的公式：

```
SST(t+n) = SST(t) + α × 大尺度趨勢 × n
```

- **大尺度趨勢**：最近 5 日逐格最小二乘斜率，再以 6.2° 箱形平均大尺度化（保留隨緯度變化的季節降溫率，濾掉雲隙與同化雜訊）。逐格斜率上限 ±0.30 °C/day。
- **α = 0.60**：收縮係數。
- 預報值對基準場的總偏離上限 ±1.5 °C；不填補陸地與原始缺值。
- **次表層水溫**：持續場 + 收縮趨勢（α=0.35），不做平流。
- **表面海流**：持續場（沿用基準日分析場）。

**選定方式**：以 2026-07-02~09-20 共 81 日連續 HIMSST 分析場，前 33 個起報日調參、後 34 個起報日驗證（分割樣本）。測試期相對持續性法的 RMSE 改進：

| 時距 | 全域 | 漁場區 (35–50°N, 140–162°E) |
| --- | ---: | ---: |
| +1 日 | +8.6% | +8.8% |
| +2 日 | +8.7% | +9.7% |
| +3 日 | +8.1% | +9.8%（RMSE 0.584 vs 0.647 °C） |

### 5.3 經量化驗證為有害而關閉的成分

這些成分直覺上合理，但實測會讓預報變差。程式碼保留於 `forecast.py`（預設關閉），並記錄數據以免日後重蹈：

| 成分 | +1 日 | +3 日 | 判讀 |
| --- | ---: | ---: | --- |
| 表面海流半拉格朗日平流 | −24.4% | −14.6% | HIMSST 是多衛星融合分析場，NPRSUBC 是獨立的 4DVAR 流場分析，兩者誤差不相關；用後者平流前者只會把鋒面搬到錯的位置 |
| 預報場 3×3 平滑 | −17.4% | −0.8% | 抹掉真實的中尺度結構 |
| 未大尺度化的逐格趨勢 | +1.1% | −4.1% | 逐格斜率主要是雲隙與同化雜訊，乘以時距後被放大 |

### 5.4 每日回溯驗證

每次建置都以當下可得的資料做滾動回溯驗證（以 D−n 預報 D，對照持續性法），結果寫入 `docs/data/manifest.json` 並直接顯示於網頁側欄與 HTML 報告。**預報若沒有比持續性法好，使用者看得到。** 由於每日滾動樣本只有數個起報日、數值會有起伏，網頁同時顯示 §5.2 的固定分割樣本基準線作為穩定對照。

### 5.5 模式降階規則

- 次表層資料相對基準日落後超過 3 日 → 全域降階為 SST 單因子。
- 163.45°E 以東（無 JMA 次表層產品）→ 該區逐格降階為 SST 單因子。
- 時效判定以**基準日**為準，不是預報有效日：預報日的次表層場本身即由基準日外推，若以預報有效日比對分析日，+3 日會被誤判為過期而在序列中途換模式，產生純屬人為的熱區跳動。
- 任何降階都會顯示在介面、圖例與報告上；不得把過舊或外推的資料靜默混入當日結果。

## 6. 操作流程

1. 先看「海況資料時效」與「預報技巧」兩個面板，確認今天的資料新鮮度與預報是否仍優於持續性法。
2. 以「當日速報 / ＋1 / ＋2 / ＋3 日」切換時距。預報場會顯示橘色橫幅與當日回溯 RMSE。
3. 開啟 SST、溫度鋒面與海流，確認黑潮／親潮交會及鋒面位置。
4. 執行「一鍵秋刀魚漁場速預報」：一次算出 D0 至 D+3 的熱區數、面積與首選熱區位移。
5. 優先檢視 HSI 較高、面積較大且鄰近溫度鋒面的連通區；注意 163.45°E 以東為 SST 單因子區。
6. 下載 HTML 速報報告；出航前再與船上魚探、天氣、海象、油耗與作業法規交叉判讀。

## 7. 驗證門檻

### 7.1 預報場（已建立）

- 每日滾動回溯驗證，對照持續性法，結果公開於網頁與報告。
- 固定分割樣本基準線（§5.2），每季以新的連續資料重新計算並更新 `config.FORECAST_BENCHMARK`。
- 若滾動驗證連續多日出現負技巧，應停用預報分頁並重新調參，不得繼續發布。

### 7.2 HSI 模式（仍待完成）

- 以年份留一法或 rolling-origin 做 2006–2010 回溯驗證，避免把同年相鄰網格同時放入訓練與測試造成空間洩漏。
- 報告 HSI 門檻下的 CPUE 捕獲覆蓋率、作業努力覆蓋率、熱區面積與可靠度曲線；因沒有真正 absence，不使用「準確率」單一指標。
- 每日檢查來源日期、缺值率、海域覆蓋率、物理合理範圍、網格方向、單位與異常跳變。
- 每季用新漁獲紀錄重估環境窗，但版本化保存門檻，不在漁季中無紀錄地調整。
- 明確標示 HSI 是決策支援，不是捕獲保證；任何安全、法規或禁漁限制均優先於模型結果。

## 8. 主要參考

- [Tseng et al. (2013), Spatial and temporal variability of the Pacific saury distribution in the northwestern Pacific Ocean](https://academic.oup.com/icesjms/article/70/5/991/643838)
- [JMA HIMSST 產品說明](https://www.data.jma.go.jp/goos/data/pub/JMA-product/him_sst_pac_D/Readme_him_sst_pac_D)
- [JMA NPRSUBT 產品說明](https://www.data.jma.go.jp/goos/data/pub/JMA-product/npr_subt_jpn_D/Readme_npr_subt_jpn_D)
- [JMA NPRSUBC 產品說明](https://www.data.jma.go.jp/goos/data/pub/JMA-product/npr_subc_jpn_D/Readme_npr_subc_jpn_D)
